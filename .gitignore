# dependencies
/node_modules
/.pnp
.pnp.js

# production
# NOTE: build/ is intentionally committed so the server can `git clone` and
# serve the prebuilt files directly (no npm install / build needed on the server).
# /build

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
